# P0 — Suzuki shared-init / matched-post audit (Step3)
# Protocol: docs/17_step3_experiment_plan.md §3
# Requires new strategies: cold_random_post, topk_random_post, topk_only
# Jobs: 12 targets × 6 strategies × 5 seeds = 360

db: data/db/transferbo2_suzuki.db

experiment_id: suzuki_p0_shared_init

n_init: 5
budget: 20

seeds: [0, 1, 2, 3, 4]

acquisition: ei
topk: 5

max_warm_points: 100
warm_strength: 0.5
lengthscale_sub: 1.0
gate_spearman_min: 0.15

use_plate_correction: false
substrate_descriptor: hashed_smiles_v1
condition_features: ohe

target_substrates: null   # all 12

strategies:
  - random              # R0
  - cold_start          # R1: cold-EI
  - topk_warm           # R2: topk-EI
  - cold_random_post    # R3: same init as R1, random post (NEW)
  - topk_random_post    # R4: same init as R2, random post (NEW)
  - topk_only           # R5: top-5 only, budget=5 (NEW)

out_dir: results/suzuki_p0_shared_init
