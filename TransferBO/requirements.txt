# TransferBO — 跨反应板 Transfer / Warm-start BO
# 建议：conda create -n transferbo python=3.10 && conda activate transferbo
#       pip install -r requirements.txt
#       pip install -e .

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
rdkit>=2023.9.1
pyyaml>=6.0
tqdm>=4.65
matplotlib>=3.7
seaborn>=0.13

# 可选 BoTorch 后端：pip install -r requirements-botorch.txt
# 可选反应指纹：drfp>=0.3

# 开发
pytest>=7.4
ipykernel>=6.25
